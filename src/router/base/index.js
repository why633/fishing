import mine from '../mine'
import userInfo from '../userInfo'
import fishingClassroom from '../fishingClassroom'
import message from '../message'

const routes = [].concat(mine, userInfo, fishingClassroom, message)

export default routes
